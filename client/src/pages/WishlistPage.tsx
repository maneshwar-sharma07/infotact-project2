import { Link } from "react-router-dom";
import EmptyState from "../components/commerce/EmptyState";
import { useCart } from "../context/CartContext";
import { useWishlist } from "../context/WishlistContext";
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;
export default function WishlistPage() { const { items, toggleItem } = useWishlist(); const { addItem } = useCart(); if (!items.length) return <main className="mx-auto min-h-screen w-full max-w-[1600px] px-4 py-12 sm:px-6 lg:px-8"><EmptyState title="Your wishlist is empty" text="Save products you love so they are easy to find later." /></main>; return <main className="mx-auto min-h-screen w-full max-w-[1600px] px-4 py-12 text-white sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-cyan-400">Saved for later</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">Wishlist</h1><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{items.map((item) => <article key={item.id} className="overflow-hidden rounded-2xl border border-gray-800 bg-[#111118]"><Link to={`/product/${item.id}`}><img src={item.imageUrl || "https://images.unsplash.com/photo-1496181133206-80ce9b88a853?auto=format&fit=crop&w=500&q=80"} alt={item.name} className="h-48 w-full object-cover" /></Link><div className="p-5"><Link to={`/product/${item.id}`} className="font-bold hover:text-cyan-300">{item.name}</Link><p className="mt-3 text-xl font-bold text-purple-400">{money(item.price)}</p><div className="mt-5 grid grid-cols-2 gap-3"><button type="button" onClick={() => addItem(item)} className="rounded-xl bg-gradient-to-r from-purple-600 to-cyan-500 px-3 py-2.5 text-sm font-semibold">Add to Cart</button><button type="button" onClick={() => toggleItem(item)} className="rounded-xl border border-gray-700 px-3 py-2.5 text-sm font-semibold text-gray-300">Remove</button></div></div></article>)}</div></main>; }
