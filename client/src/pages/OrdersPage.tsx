import { Link } from "react-router-dom";
import EmptyState from "../components/commerce/EmptyState";
import type { StoredOrder } from "../types/ecommerce";
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;
export default function OrdersPage() { const orders = JSON.parse(localStorage.getItem("shopsphere-orders") || "[]") as StoredOrder[]; if (!orders.length) return <main className="mx-auto min-h-screen w-full max-w-[1600px] px-4 py-12 sm:px-6 lg:px-8"><EmptyState title="No orders yet" text="When you place an order, the details will appear here." /></main>; return <main className="mx-auto min-h-screen w-full max-w-[1200px] px-4 py-12 text-white sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-cyan-400">Account</p><h1 className="mt-2 text-3xl font-bold">Order History</h1><div className="mt-8 space-y-4">{orders.map((order) => <article key={order.id} className="rounded-2xl border border-gray-800 bg-[#111118] p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4 border-b border-gray-800 pb-4"><div><p className="font-bold">Order {order.id}</p><p className="mt-1 text-sm text-gray-400">{new Date(order.placedAt).toLocaleDateString("en-IN", { dateStyle: "medium" })}</p></div><div className="text-right"><span className="rounded-full bg-emerald-400/10 px-3 py-1 text-xs font-semibold text-emerald-300">{order.status}</span><p className="mt-3 font-bold text-purple-400">{money(order.total)}</p></div></div><div className="mt-4 space-y-2">{order.items.map((item) => <Link key={item.id} to={`/product/${item.id}`} className="flex justify-between text-sm text-gray-300 hover:text-cyan-300"><span>{item.name} × {item.quantity}</span><span>{money(item.price * item.quantity)}</span></Link>)}</div></article>)}</div></main>; }
