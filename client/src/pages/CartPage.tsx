import { Link } from "react-router-dom";
import EmptyState from "../components/commerce/EmptyState";
import { useCart } from "../context/CartContext";
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;
export default function CartPage() { const { items, total, updateQuantity, removeItem, clearCart } = useCart(); if (!items.length) return <Page><EmptyState title="Your cart is waiting" text="Add something you love and it will appear here." /></Page>; return <Page><div className="grid gap-8 lg:grid-cols-[1fr_360px]"><section><div className="mb-6 flex items-center justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-cyan-400">Shopping bag</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">Your Cart</h1></div><button type="button" onClick={clearCart} className="text-sm font-semibold text-red-300 transition hover:text-red-200">Empty cart</button></div><div className="space-y-4">{items.map((item) => <article key={item.id} className="flex gap-4 rounded-2xl border border-gray-800 bg-[#111118] p-4 sm:p-5"><img src={item.imageUrl || "https://images.unsplash.com/photo-1496181133206-80ce9b88a853?auto=format&fit=crop&w=300&q=80"} alt="" className="h-24 w-24 rounded-xl object-cover" /><div className="min-w-0 flex-1"><Link to={`/product/${item.id}`} className="font-bold transition hover:text-cyan-300">{item.name}</Link><p className="mt-1 text-sm text-gray-400">{item.category}</p><p className="mt-3 font-bold text-purple-400">{money(item.price)}</p></div><div className="flex flex-col items-end justify-between"><button type="button" onClick={() => removeItem(item.id)} className="text-sm text-gray-500 transition hover:text-red-300">Remove</button><div className="flex items-center rounded-xl border border-gray-700"><button type="button" onClick={() => updateQuantity(item.id, item.quantity - 1)} className="px-3 py-2 text-gray-300">−</button><span className="min-w-9 text-center text-sm font-semibold">{item.quantity}</span><button type="button" onClick={() => updateQuantity(item.id, item.quantity + 1)} className="px-3 py-2 text-gray-300">+</button></div></div></article>)}</div></section><aside className="h-fit rounded-2xl border border-gray-800 bg-[#111118] p-6 lg:sticky lg:top-28"><h2 className="text-xl font-bold">Order Summary</h2><div className="mt-6 space-y-3 border-b border-gray-800 pb-5 text-gray-400"><p className="flex justify-between"><span>Subtotal</span><span>{money(total)}</span></p><p className="flex justify-between"><span>Shipping</span><span className="text-emerald-300">Free</span></p></div><p className="mt-5 flex justify-between text-xl font-bold"><span>Total</span><span className="text-purple-400">{money(total)}</span></p><Link to="/checkout" className="mt-6 block rounded-xl bg-gradient-to-r from-purple-600 to-cyan-500 py-3.5 text-center font-semibold transition hover:brightness-110">Proceed to checkout</Link></aside></div></Page>; }
function Page({ children }: { children: React.ReactNode }) { return <main className="mx-auto min-h-screen w-full max-w-[1600px] px-4 py-12 text-white sm:px-6 lg:px-8">{children}</main>; }
