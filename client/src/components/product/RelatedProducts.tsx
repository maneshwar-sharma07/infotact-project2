import { Link } from "react-router-dom";
import type { StoreProduct } from "../../types/ecommerce";
type RelatedProductsProps = { products: StoreProduct[] };
const fallbackImage = "https://images.unsplash.com/photo-1496181133206-80ce9b88a853?auto=format&fit=crop&w=900&q=85";
export default function RelatedProducts({ products }: RelatedProductsProps) { if (!products.length) return null; return <section className="mt-16"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-cyan-400">More to explore</p><h2 className="mt-2 text-3xl font-bold text-white">Related Products</h2><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map((product) => <Link key={product.id} to={`/product/${product.id}`} className="group overflow-hidden rounded-2xl border border-gray-800 bg-[#111118] shadow-xl shadow-black/20 transition duration-300 hover:-translate-y-1 hover:border-purple-500/70 hover:shadow-purple-950/30"><div className="overflow-hidden bg-[#171722]"><img src={product.imageUrl || fallbackImage} alt={product.name} className="h-48 w-full object-cover transition duration-500 group-hover:scale-105" /></div><div className="p-4"><p className="text-xs font-semibold uppercase tracking-wider text-cyan-400">{product.category}</p><h3 className="mt-2 truncate font-bold text-white">{product.name}</h3><p className="mt-3 text-xl font-bold text-purple-400">&#8377;{product.price.toLocaleString("en-IN")}</p></div></Link>)}</div></section>; }
